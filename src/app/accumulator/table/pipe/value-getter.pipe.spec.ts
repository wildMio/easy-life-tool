import { ValueGetterPipe } from './value-getter.pipe';

describe('ValueGetterPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueGetterPipe();
    expect(pipe).toBeTruthy();
  });
});
