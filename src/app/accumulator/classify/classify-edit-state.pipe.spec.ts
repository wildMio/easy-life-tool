import { ClassifyEditStatePipe } from './classify-edit-state.pipe';

describe('ClassifyEditStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ClassifyEditStatePipe();
    expect(pipe).toBeTruthy();
  });
});
